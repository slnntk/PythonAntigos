primeiro = int(input('Primeiro numero: '))
segundo  = int(input('Segundo numero : '))
terceiro = int(input('Terceiro numero: '))
quarto = int(input('quarto numero: '))
quinto = int(input('quinto numero: '))
sexto = int(input('sexto numero: '))
setimo = int(input('setimo numero: '))
oitavo = int(input('oitavo numero: '))
nono = int(input('nono numero: '))
decimo = int(input('decimo numero: '))

    # Achando o maior número
maior = primeiro

if (segundo > maior):
        maior = segundo
if (terceiro > maior):
        maior = terceiro
if (quarto > maior):
        maior = quero
if (quinto > maior):
        maior = quinto
if (sexto > maior):
        maior = sexto
if (setimo > maior):
        maior = setimo
if (oitavo > maior):
        maior = oitavo
if (nono > maior):
       maior = nono
if (decimo > maior):
        maiorr = decimo

print('Maior: ',maior)

    # Achando o menor número
menor = primeiro

if (segundo < menor):
        menor = segundo
if (terceiro < menor):
        menor = terceiro
if (quarto < menor):
        menor = quero
if (quinto < menor):
        menor = quinto
if (sexto < menor):
        menor = sexto
if (setimo < menor):
        menor = setimo
if (oitavo < menor):
        menor = oitavo
if (nono < menor):
        menor = nono
if (decimo < menor):
        menor = decimo

print('Menor: ',menor)
